function computeMultiplesSum(n) {
    let array = [];

    if (0 <= n < 1000) {
        for(let i=0; i < n ; i++){
            if( i%n == 0 )[
                array.push(i)
            ]
        }
    }
}